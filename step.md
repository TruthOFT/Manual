## 2026-04-25

- 新增 `recommendation_cf_tables.sql`，补充基于物品协同过滤推荐所需的用户商品行为、商品相似度和用户商品推荐结果表结构。

- 优惠券领取规则改为用户只要领取过同一张券就不能再次领取，用户端可领取优惠券卡片隐藏剩余/总数等数量信息。
- 前台商品详情页材料信息区域调整空状态逻辑：已有材质说明时不再额外显示“暂无材料明细”。
- 后台优惠券表单按类型切换优惠字段：满减券只填写优惠金额，折扣券只填写折扣比例；保存时后端同步清理未使用字段。
- 后台优惠券“发放”入口改回领取记录查看语义，移除后台主动选择用户发放控件，弹窗仅展示谁领取了该优惠券并支持刷新记录。
- 后台优惠券发放弹窗将顾客用户 id 输入框改为可搜索顾客下拉选择，支持按账号、姓名、手机号加载启用顾客后发放。
- 优惠券库存口径调整为使用时扣减：剩余数量按 `totalCount - usedCount` 计算，领取/后台发放只增加领取数，同一用户用完后可再次领取同券但不能重复持有未使用券。
- 订单列表接口和前端展示新增 SKU 图片优先级，订单页明确优先使用 `skuCover`；后台保存 SKU 时不再把商品主图回填进 SKU 图片字段。
- 用户订单明细图片改为优先展示购买 SKU 的图片：创建订单快照写入 SKU 图，读取历史订单时也通过 SKU 图兜底回显。
- 后台商品保存去掉“仅审核通过的商品才可以上架”的前后端校验，上架/下架只受商品状态和可购买 SKU 约束。
- 后台商品管理移除商品列表表格里的“审核状态”列，并删除新增/编辑商品弹窗里的“审核状态”下拉项。
- 后台商品管理移除商品列表操作列和商品详情抽屉里的“驳回”操作入口，保留历史驳回状态的只读展示。
- 后台商品管理的商品上架状态收敛为“上架/下架”，移除上架状态里的“草稿”，后端兼容历史 `status=2` 为下架并保存为 `status=0`。

- 前台商品详情页左侧主图改为跟随当前选中的 SKU 图片展示，SKU 未配置图片时回退商品封面，并将 SKU 图片补入缩略图预览列表。

- 前台商品详情页删除“评价与反馈”模块，材料信息区域改为独占整行展示。
- 后台商品新增/编辑补充 SKU 规格维护，表单支持添加、删除和编辑规格名、规格描述、材质、重量、售价、原价、库存和启停状态。
- 后端商品保存同步写入 `product_sku`，编辑商品时刷新规格数据，并在商品上架前校验至少存在一个启用且有库存的可购买规格。
- 后台新增商品默认设为已审核并上架，配合默认规格让新增商品保存后可直接在前台购买。
- 下单购买校验改为以 SKU 启用状态、库存和价格为准，不再因为商品主表状态默认值拦截可用规格。
- 后台商品表单封面和 SKU 图片改为本地暂存预览，保存时先上传图片获取地址，再随商品和规格数据写入数据库。
- 修复前台商品页无数据问题：公开商品查询先读取已审核上架商品，若为空则回退展示未删除且分类可用的商品，同时首页商品列表使用相同回退逻辑。
- 前台商品页商品封面接入上传路径解析，保证 `/upload/...` 图片可以通过后端静态资源接口正常显示。

- 后台用户管理编辑弹窗将“头像地址”改为“头像”图片选择预览，支持删除当前头像、重新选择本地图片，并在保存前先上传 `user` 图片再提交用户新增/更新。
- 后端用户更新保留空头像字符串写入能力，确保后台删除头像后数据库字段可以同步清空。

## 2026-04-24

- 店员资料接入工资 `salary` 字段，后端 DTO/VO/mapper/service 支持新增、编辑、查询工资，后台店员管理列表和表单增加工资展示与维护。

- 下线匠人相关后端控制器，避免 `/artisans`、`/artisan-center`、`/artisan-application`、`/admin/artisan-applications` 继续进入旧匠人表查询逻辑。
- 修复后台管理布局、用户管理、顾客管理、店员管理、优惠券管理页面的 UTF-8 中文文案和断裂模板语法。
- 修复前台顶部导航和个人中心布局，保留首页、商品、个人中心入口，移除匠人故事、定制服务、匠人工作台、申请成为匠人入口。
- 删除前台匠人/定制相关 API、类型、页面和匠人工作台布局文件，删除后台匠人审核 API、类型和页面文件。
- 删除后端匠人/定制相关 service、mapper、dto、vo、entity 和对应旧控制器测试，清理 `artisan_profile`、`custom_requirement`、`artisanId` 旧表旧字段查询来源。
- 清理前台个人中心 mock 中的店铺/定制文案，移除前台商品列表和详情页的定制标签展示，更新首页和用户测试里的旧匠人断言/样例账号。
- 隐藏后台商品详情和编辑表单里的定制文案，保留数据库字段默认值以兼容现有商品结构。
- 重写优惠券 mapper 的 SQL 注解，修复损坏的字符串文本块导致的后端编译错误。
- 修复商品控制器测试中的乱码字符串，保证测试源码使用 UTF-8 可编译中文。
- 移除 `ManualApplication` 上的全局 `@MapperScan`，改用 mapper 接口自身的 `@Mapper` 注解，避免 WebMvcTest 切片误加载数据库 mapper。
- 新增普通用户优惠券领取查看闭环：`/coupons/available`、`/coupons/{couponId}/receive`、`/coupons/my`，基于当前登录普通用户查询可领取、主动领取和已领取优惠券。
- 新增前台个人中心“我的优惠券”页面、路由、菜单、API 和类型，支持可领取/已领取两类视图，暂不接入下单抵扣。
- 调整后台店员新增逻辑，创建时由后端使用雪花算法生成 `staffNo`，前端新增店员表单不再手填员工编号，编辑时只读展示原编号。
- 补充用户优惠券控制器/service 测试和店员创建 service 测试，覆盖领取接口、重复领取、库存不足、领取写入、未登录/无权限和员工编号自动生成场景。
- 接入提交订单优惠券选择：订单创建请求新增 `couponReceiveId`，后端校验本人未使用优惠券、有效期和门槛，计算 `discountAmount`/`payAmount` 并标记领取记录已使用。
- 订单取消和超时取消时恢复关联优惠券为未使用并回退 `usedCount`，避免待支付订单取消后优惠券被永久占用。
- 前台商品详情确认订单弹窗新增可用优惠券选择、优惠金额和应付金额展示，提交订单时携带所选优惠券领取记录。
- 后台分类管理编辑弹窗将分类图标地址输入改为图片选择预览，保存时先上传 `category` 图片，再把返回地址写入分类新增/更新请求。
- 修复分类图标上传控件中原生文件输入与自定义按钮同时显示的问题，只保留一个可见的“选择图片”入口。
- 修复前台首页分类、商品和订单图片路径解析，使用上传地址补全逻辑让 `/upload/...` 图片能通过后端 `/api/upload/...` 正常显示。
- 后台店员新增/编辑的入职时间、优惠券新增/编辑的开始时间和结束时间改为日期时间选择器，提交格式保持 `yyyy-MM-dd HH:mm:ss`。
- 后台仪表盘移除“待审核商品”统计卡片和今日待办里的商品审核项。
